{
  "name": "nyc-rent-seekers",
  "version": "0.1.0",
  "private": true,
  "description": "NYC Rent Seekers — evidence-first rent-wedge map (standalone)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:browser": "node tests/browser/smoke.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vite-plugin-singlefile": "^2.1.0",
    "playwright": "^1.50.1"
  },
  "engines": {
    "node": ">=20"
  }
}
